# Field Notes is a fully static site: there are NO required environment
# variables and NO secrets. Everything below is optional.

# Port used by `astro dev` / `astro preview` when --port is not passed.
PORT=4321

# Host to bind the dev/preview server to (defaults to 127.0.0.1 via scripts).
HOST=127.0.0.1